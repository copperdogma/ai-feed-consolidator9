import { Pane, Heading } from 'evergreen-ui'

const NotFound = () => (
  <Pane>
    <Heading size={900} fontWeight={400}>
      Not Found
    </Heading>
  </Pane>
)

NotFound.displayName = 'NotFound'

export default NotFound
